[package]
name = "moreutils"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4"
lexopt = "0.3"
regex = "1.11"
signal-hook = { version = "0.3", features = ["channel","iterator","extended-siginfo"] }
sysinfo = "0.36"
